function extractFirstCharacters() {
    let str = "Hello World";
    let first5Characters = str.substring(0, 5); // Extracts characters from index 0 to 4
    
    alert("First 5 characters: '" + first5Characters + "'");
}
